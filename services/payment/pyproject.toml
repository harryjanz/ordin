[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.coverage.run]
source = ["."]
omit = ["tests/*", "migrations/*", "conftest.py", "generate_openapi.py"]

[tool.coverage.report]
fail_under = 80
show_missing = true
